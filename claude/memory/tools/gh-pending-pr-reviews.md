# gh + GraphQL: incrementally building a pending PR review

**Promoted to a skill (2026-06-01).** Content now lives in the `pending-pr-review` skill at
`~/.claude/skills/pending-pr-review/SKILL.md` (versioned in dotfiles at `claude/skills/`).

It auto-surfaces on PR-review requests and is also invocable as `/pending-pr-review`.

When the workflow needs updating, edit the SKILL.md rather than this file.

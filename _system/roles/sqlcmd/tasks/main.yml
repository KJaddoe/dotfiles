---
- name: Install prerequisites (Ubuntu)
  ansible.builtin.apt:
    name:
      - curl
      - gnupg
      - ca-certificates
      - apt-transport-https
      - python3-debian
    state: present
    update_cache: yes
  become: true
  when: ansible_facts.os_family == "Debian"

- name: Create keyrings directory (Ubuntu)
  become: true
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    mode: "0755"
  when: ansible_facts.os_family == "Debian"

- name: Add Microsoft signing key (Ubuntu)
  become: true
  ansible.builtin.get_url:
    url: https://packages.microsoft.com/keys/microsoft.asc
    dest: /etc/apt/keyrings/microsoft.asc
    mode: "0644"
  when: ansible_facts.os_family == "Debian"

- name: Add Microsoft SQL Server repository (Ubuntu)
  become: true
  ansible.builtin.deb822_repository:
    name: microsoft-prod
    types: deb
    uris: https://packages.microsoft.com/ubuntu/{{ ansible_facts.distribution_version }}/prod
    suites: "{{ ansible_facts.distribution_release }}"
    components: main
    architectures: amd64
    signed_by: /etc/apt/keyrings/microsoft.asc
    state: present
  when: ansible_facts.os_family == "Debian"

- name: Install sqlcmd tools (Ubuntu)
  become: true
  environment:
    ACCEPT_EULA: 'Y'
  ansible.builtin.apt:
    name:
      - mssql-tools18
      - unixodbc-dev
    state: present
    update_cache: yes
  when: ansible_facts.os_family == "Debian"

- name: Tap Microsoft MSSQL repo (macOS)
  community.general.homebrew_tap:
    name: microsoft/mssql-release
    state: present
  when: ansible_facts.os_family == "Darwin"

- name: Install sqlcmd (macOS)
  community.general.homebrew:
    name: sqlcmd
    state: present
  when: ansible_facts.os_family == "Darwin" 

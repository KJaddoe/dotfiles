let mapleader = " "         " Spacebar map leader
nmap , :

" mappings: for long lines over multiple rows, do proper navigation
map j gj
map k gk

" Easier navigations through windows
nnoremap <C-J> <C-W><C-J>
nnoremap <C-K> <C-W><C-K>
nnoremap <C-L> <C-W><C-L>
nnoremap <C-H> <C-W><C-H>

" Close the current buffer and move to the previous one
nmap <leader>bq :bp <BAR> bd #<CR>

" ==================== Fugitive ====================
nnoremap <leader>ga :Git add %:p<CR><CR>
nnoremap <leader>gs :Gstatus<CR>
nnoremap <leader>gp :Gpush<CR>
nnoremap <leader>gc :Gcommit<CR>
vnoremap <leader>gb :Gblame<CR>"

" settings nerdtree(tabs): always open and focus on files if opening
noremap <Leader>e :NERDTreeToggle<cr>
noremap <Leader>f :NERDTreeFind<cr>

" Easy align settings
" Start interactive EasyAlign in visual mode (e.g. vip<Enter>)
vmap <Enter> <Plug>(EasyAlign) 
" Start interactive EasyAlign for a motion/text object (e.g. gaip)
nmap ga <Plug>(EasyAlign)

" Tagbar 
nnoremap <silent> <Leader>T :TagbarToggle<CR>

" Vimux 
nmap <leader>rm :VimuxRunCommand './make.sh'<CR>
nmap <leader>rc :VimuxRunCommand 'make clean'<CR>
nmap <leader>rt :VimuxRunCommand './make.sh && ./test.sh'<CR>
nmap <leader>rl :VimuxRunCommand './latexmk.sh'<CR>

" Doxygen
nnoremap <leader>d :Dox<CR>

" Autocomplete
autocmd FileType javascript let g:SuperTabDefaultCompletionType = "<c-x><c-o>"
autocmd FileType typescript let g:SuperTabDefaultCompletionType = "<c-x><c-o>"
let g:UltiSnipsExpandTrigger="<C-j>"
inoremap <expr><TAB>  pumvisible() ? "\<C-n>" : "\<TAB>"

imap <expr> <CR> pumvisible() ? "\<c-y>" : "<Plug>delimitMateCR"
map <F7> mzgg=G`z
